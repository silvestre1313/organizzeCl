<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.organizze">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_organizze"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_organizze"
        android:supportsRtl="true"
        android:theme="@style/Theme.Organizze">
        <activity android:name=".activity.ReceitasActivity" android:theme="@style/receitasTheme"></activity>
        <activity android:name=".activity.DespesasActivity" android:theme="@style/despesasTheme" />
        <activity
            android:name=".activity.PrincipalActivity"
            android:label="@string/title_activity_principal"
            android:theme="@style/Theme.Organizze.NoActionBar" />
        <activity android:name=".activity.CadastroActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/Theme.Intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>